{
  "name": "bloomknights",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Slices 1-2: end-to-end skeleton with pluggable scoreboard extraction — frame -> parsed state -> probability -> mock market -> comparison",
  "scripts": {
    "start": "node services/api/server.js",
    "test": "node --test 'tests/*.test.js'",
    "evaluate": "node scripts/evaluate-fixtures.js"
  }
}
